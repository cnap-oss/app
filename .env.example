# Copy to .env and fill with your real values. Keep .env files untracked.

# ==================== Required Configuration ====================

# Discord Bot Token (required)
DISCORD_TOKEN=your-discord-bot-token

# OpenCode API Key (required)
OPENCODE_API_KEY=your-opencode-api-key

# ==================== Database Configuration ====================

# Database connection URL
# For PostgreSQL: postgres://user:password@host:port/database?sslmode=disable
# For SQLite: leave empty (will use $CNAP_DIR/cnap.db)
DATABASE_URL=postgres://cnap:cnap@localhost:5432/cnap?sslmode=disable

# Database logging and connection pool settings (optional)
# DB_LOG_LEVEL=warn
# DB_MAX_IDLE=5
# DB_MAX_OPEN=20
# DB_CONN_LIFETIME=30m
# DB_SKIP_DEFAULT_TXN=true
# DB_PREPARE_STMT=false

# ==================== Application Configuration ====================

# Runtime environment (development, production)
# In production, runner images are pulled from ghcr.io
ENV=production

# Application log level (debug, info, warn, error)
LOG_LEVEL=info

# ==================== API Keys (Optional) ====================

# Additional AI provider API keys
# ANTHROPIC_API_KEY=your-anthropic-api-key
# OPENAI_API_KEY=your-openai-api-key

# ==================== Runner Configuration ====================

# Docker image for task runners
# If not set, defaults to:
#   - production: ghcr.io/cnap-oss/cnap-runner:latest
#   - development: cnap-runner:latest
# RUNNER_IMAGE=ghcr.io/cnap-oss/cnap-runner:v1.0.0

# Runner workspace directory (optional)
# RUNNER_WORKSPACE_DIR=/custom/workspace/path

# ==================== Directory Configuration ====================

# Base data directory (default: $HOME/.cnap)
# All data (workspace, messages, configs, database) will be stored under this directory
# CNAP_DIR=/custom/path/to/cnap

# Override specific directories (optional)
# WORKSPACE_BASE_DIR=/custom/workspace/path
# SQLITE_DATABASE=/custom/database/path/cnap.db
